version: "3.5"
services:
  mysql8:
    container_name: mysql8-google
    hostname: mysql
    image: mysql:8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 1234
    ports:
      - "3308:3306"
    volumes:
      - ./database:/var/lib/mysql
    networks:
      - net-db
    

  mysql8admin:
    image: phpmyadmin/phpmyadmin
    container_name: mysql8admin-google
    hostname: mysql8admin
    restart: always
    links:
      - mysql8:db
    ports:
      - 4402:80
    networks:
      - net-db

networks:
    net-db:
        external: true


